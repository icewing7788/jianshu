import React,{PureComponent}from 'react';
import {WritterWrapper} from '../style';

class Writer extends PureComponent{
    render() {
        return (
            <WritterWrapper>二维码</WritterWrapper>
        )
    }
}

export default Writer;